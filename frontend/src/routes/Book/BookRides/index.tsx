import { createFileRoute } from '@tanstack/react-router'
import BookRides from '../../../UI/BookRides/BookRides'

export const Route = createFileRoute('/Book/BookRides/')({
  component: BookRides,
})

