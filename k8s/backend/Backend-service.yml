apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: jausathe-namespace

spec: 
  type:  ClusterIP
  selector:
    app: backend 
  ports:
    - name: backend-service
      port: 8000
      targetPort: 8000
      protocol: TCP
      
