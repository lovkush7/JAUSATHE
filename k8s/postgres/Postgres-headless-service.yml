apiVersion: v1
kind: Service
metadata:
  name: postgres-headless-service
  namespace: jausathe-namespace

spec:  
  clusterIP: None
  selector:
    app: postgres

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP  
      