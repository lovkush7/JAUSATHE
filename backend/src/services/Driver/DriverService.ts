import type { Driverdto } from "../../dto/Driver.dto.ts";

class DriverServices {
    async createDriver(
        userId: string,
        body: Driverdto,
    ){

    }
}
export default new DriverServices();